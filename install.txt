feat: 백엔드 관제탑(MainProcessor) 도입 및 자연어 검색 파이프라인 통합

[주요 변경 사항]
- 아키텍처 개편: 
  - MainProcessor 관제탑 모듈 추가하여 파일 전처리, AI 분석, DB 저장을 비즈니스 로직으로 분리.
  - GUI 스레드와 백엔드 처리 로직을 완전히 격리하여 UI 응답성 개선.

- 자연어 파싱 및 검색 고도화:
  - QueryParseWorker, SearchEngine 도입으로 자연어 의도(@검색, @대화) 파싱 구현.
  - 지능형 검색 엔진 추가: 불용어(Stopwords) 필터링, 동의어(Synonym) 사전 확장, 검색 0건 방지용 폴백(Fallback) 검색 로직 구현.

- 안정성 보완:
  - 윈도우 경로 인코딩 정규화(￥ 처리 등) 및 경로 문제 근본 해결.
  - DB 초기화 로직 변경: os.remove 대신 SQL DELETE 방식을 적용하여 WinError 32(프로세스 점유 에러) 차단.

[용어 및 개념 설명]
- 자연어(Natural Language): 인간이 일상적으로 사용하는 언어. 이를 컴퓨터가 이해 가능한 구조화된 데이터(JSON)로 변환하는 '자연어 처리(NLP)'를 적용했습니다.
- 불용어(Stopwords): 검색 시 의미가 적은 단어(예: '파일', '찾아줘'). 검색 품질 향상을 위해 필터링합니다.
- 동의어 확장(Synonym Expansion): '전쟁' 검색 시 '대전', '전투'까지 검색되도록 유의어를 확장하여 누락을 방지합니다.
- 폴백(Fallback) 검색: 검색 결과 0건 발생 시, 검색 조건을 완화하여 연관 데이터를 찾아주는 안전장치입니다.

[시스템 아키텍처 및 요구 사항]
- 데이터 통신: 모든 AI 분석 결과 및 라우팅 메시지는 명확한 구조를 보장하기 위해 'JSON' 규격으로 통신합니다.
- 필수 환경 설정(Dependencies):
  1. Ollama 설치: 텍스트 분석용 'qwen2.5:3b', 비전 모델 'llava' 모델을 반드시 pull 해야 합니다.
  2. FFmpeg 설치: 미디어 파일(mp3, mp4 등) 음성 인식을 위한 필수 엔진이며 시스템 환경 변수 등록이 필요합니다.
  3. Python 라이브러리: PySide6, pypdf, python-pptx, olefile, openai-whisper 등 프로젝트 의존성 패키지 설치가 필수입니다.

[기타]
- 전체 코드 주석 보강 및 가독성 최적화.